export interface Book {
    isbn: string
    authors: string
    numberOfPages: number
    publisher: string
    country: string
    mediaType: string
    released: string
    characters: string[]
    name: string
    url: string
    povCharacters: string[]
}
